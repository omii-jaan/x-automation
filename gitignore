# Secrets — NEVER commit
cookies.json
browser_cookies.json
.env
*.key

# Python
__pycache__/
*.py[cod]
*.so
.Python
*.egg-info/
.pytest_cache/
.venv/
venv/

# Logs (only committed by GitHub Actions bot)
logs/*.log

# OS
.DS_Store
Thumbs.db
